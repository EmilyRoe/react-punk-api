import React from 'react';
import FeedbackPanel from "../FeedbackPanel";

const NotFound = () => {
  return (
    <FeedbackPanel header="Beer not found" text="Unfortunately we do not have that beer available"/>
  )
}

export default NotFound
